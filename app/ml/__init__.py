"""PulseFi offline machine-learning helpers.

These helpers are intentionally safe to import from scripts/tests because they do
not load app.core.config, .env, database connections, or production secrets.
"""
